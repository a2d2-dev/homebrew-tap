# claudecm tap and bucket

Homebrew:

```bash
brew tap a2d2-dev/tap
brew install claudecm
```

Scoop:

```powershell
scoop bucket add a2d2-dev https://github.com/a2d2-dev/homebrew-tap
scoop install claudecm
```
